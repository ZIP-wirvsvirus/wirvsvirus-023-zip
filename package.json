{
  "name": "team23zip_fullstack",
  "version": "0.0.0",
  "scripts": {
    "heroku-prebuild": "cd angular_src && npm install && ng set --global warnings.packageDeprecation=false && npm install -g angular-cli && npm run ng build",
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "test": "ng test",
    "lint": "ng lint",
    "e2e": "ng e2e"
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  },
  "engines": {
    "node": "12.X.X"
  }
}
